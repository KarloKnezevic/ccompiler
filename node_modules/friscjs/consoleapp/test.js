    var input = "";

    process.stdin.on('data', function(chunk) {
        console.log("Chunk:", chunk.toString());
        input += chunk;
    });

    process.stdin.on('close', function() {
        console.log("End!");

        // process input
    });
