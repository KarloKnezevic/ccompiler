; ===========================================================================
; PPJ Compiler - Generated FRISC Assembly Code
; Source: program.c
; Generated: 2026-02-05 09:32:06
; ===========================================================================
; ===========================================================================
; Program entry point and initialization
; ===========================================================================
        MOVE 40000, R7          ; Initialize stack pointer (SP)
        CALL F_MAIN             ; Call main
        HALT                    ; Program end
; ===========================================================================
; Function Definitions
; ===========================================================================
F_F                             ; Function: f
        PUSH R5                 ; Save old FP
        MOVE R7, R5             ; Set FP
        SUB R7, 0C, R7          ; Allocate locals/temps
        MOVE 3, R1              ; Zero words
        MOVE R7, R0             ; Zero ptr
        MOVE 0, R2              ; Zero
L_ZERO_2
        STORE R2, (R0)          ; Clear
        ADD R0, 4, R0
        SUB R1, 1, R1
        JP_NE L_ZERO_2
L_F_L0
        MOVE R5, R0
        ADD R0, 8, R0           ; Param address
        STORE R0, (R5-4)        ; Store temp t0
        LOAD R0, (R5-4)         ; Load temp t0
        LOAD R0, (R0)           ; Load word
        STORE R0, (R5-8)        ; Store temp t1
        LOAD R0, (R5-8)         ; Load temp t1
        PUSH R0                 ; Save base address
        MOVE 3, R0
        MOVE R0, R1             ; Index
        POP R0                  ; Restore base
        SHL R1, 2, R1           ; Index * 4
        ADD R0, R1, R0          ; Base + offset
        STORE R0, (R5-0C)       ; Store temp t2
        LOAD R0, (R5-0C)        ; Load temp t2
        PUSH R0                 ; Save address
        MOVE 7B, R0
        POP R1                  ; Restore address
        STORE R0, (R1)          ; Store word
        MOVE 0, R6              ; Return 0 (void)
        JP L_EXIT_F_F_1
L_EXIT_F_F_1                    ; Function epilogue
        ADD R7, 0C, R7          ; Deallocate locals/temps
        POP R5                  ; Restore FP
        RET                     ; Return
F_MAIN                          ; Function: main
        PUSH R5                 ; Save old FP
        MOVE R7, R5             ; Set FP
        SUB R7, 24, R7          ; Allocate locals/temps
        MOVE 9, R1              ; Zero words
        MOVE R7, R0             ; Zero ptr
        MOVE 0, R2              ; Zero
L_ZERO_4
        STORE R2, (R0)          ; Clear
        ADD R0, 4, R0
        SUB R1, 1, R1
        JP_NE L_ZERO_4
L_MAIN_L0
        MOVE R5, R0
        SUB R0, 14, R0          ; Local address
        STORE R0, (R5-18)       ; Store temp t0
        LOAD R0, (R5-18)        ; Load temp t0
        STORE R0, (R5-24)       ; Save arg
        SUB R7, 4, R7           ; Allocate args
        LOAD R0, (R5-24)        ; Load arg
        STORE R0, (R7)          ; Store arg
        CALL F_F                ; Call f
        ADD R7, 4, R7           ; Clean args
        LOAD R0, (R5-18)        ; Load temp t0
        PUSH R0                 ; Save base address
        MOVE 3, R0
        MOVE R0, R1             ; Index
        POP R0                  ; Restore base
        CMP R1, 0               ; Bounds check
        JP_SLT L_BOUNDS_ERROR
        CMP R1, 5
        JP_SGE L_BOUNDS_ERROR
        SHL R1, 2, R1           ; Index * 4
        ADD R0, R1, R0          ; Base + offset
        STORE R0, (R5-1C)       ; Store temp t1
        LOAD R0, (R5-1C)        ; Load temp t1
        LOAD R0, (R0)           ; Load word
        STORE R0, (R5-20)       ; Store temp t2
        LOAD R0, (R5-20)        ; Load temp t2
        MOVE R0, R6             ; Set return value
        JP L_EXIT_F_MAIN_3
L_EXIT_F_MAIN_3                 ; Function epilogue
        ADD R7, 24, R7          ; Deallocate locals/temps
        POP R5                  ; Restore FP
        RET                     ; Return
L_BOUNDS_ERROR                  ; array bounds error
        MOVE -6, R6             ; Error code
        HALT                    ; Abort
; ===========================================================================
; Global Variables
; ===========================================================================
